/* exported capitalize */

function capitalize(word) {
  return word[0].toUpperCase() + word.slice(1).toLowerCase();
}

// used a method to make the first letter of the string uppercase
// use the method to split the string from its first index and second index
// use a method to make the remaining letter lower case
// concatenated both values
