/* exported getLastChar */
function getLastChar(string) {
  var lastChar = string.length - 1;
  var lastLetter = string[lastChar];
  return lastLetter;
}

// use the string.length and subtract 1 to get the last letter of the string
