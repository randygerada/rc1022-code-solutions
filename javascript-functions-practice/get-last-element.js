/* exported getLastElement */
function getLastElement(array) {
  var newArray = array;
  var lengthOfArray = newArray.length;
  var lastIndex = lengthOfArray - 1;
  var lastElement = newArray[lastIndex];
  return lastElement;
}
