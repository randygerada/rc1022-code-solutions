/* exported head */
// need to retrieve the first element in the array.
// have to use a for loop in order to go through the whole array
// use the length of the array as a condition.

function head(array) {
  return array[0];
}
