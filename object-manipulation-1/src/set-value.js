/* exported setValue */

// 3 parameters
// creating a function to add to an object
// adding a string property value
// value of propety would also equal string

// object is object
// key = to property
// value = to the value of the proprty

function setValue(object, key, value) {
  object[key] = value;
}
